name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    environment: drive-file-app
    env:
      SINGLESTORE_USER: ${{ secrets.SINGLESTORE_USER }}
      SINGLESTORE_PASS: ${{ secrets.SINGLESTORE_PASS }}
      SINGLESTORE_Host: ${{ secrets.SINGLESTORE_HOST }}
      SINGLESTORE_Port: ${{ secrets.SINGLESTORE_PORT }}
      SINGLESTORE_DB: ${{ secrets.SINGLESTORE_DB }}
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Install Dependencies
        run: npm install -g pnpm && pnpm install

      - name: Copy .env.example files
        shell: bash
        run: find . -type f -name ".env.example" -exec sh -c 'cp "$1" "${1%.*}"' _ {} \;

      - name: Typecheck
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint
